$('#f-calendar').fullCalendar('removeEvents', [<%= @period.id %>]);
$('#f-calendar').fullCalendar(
  'renderEvent', 
  // $.parseJSON("<%= escape_javascript render(:partial => 'periods/_period.json.jbuilder', :locals => { :period => @period }) %>")
  $.parseJSON("<%=j render(@period, format: :json).html_safe %>"), 
  true
);
$('.modal').modal('hide');
