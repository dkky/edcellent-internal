$('#f-calendar').fullCalendar('removeEvents', [<%= @period.id %>]);
$('#f-calendar').fullCalendar('renderEvent', 
  $.parseJSON("<%= raw render partial: 'periods/period.json.jbuilder', locals: { period: @period } %>"),
);
$('.modal').modal('hide');
 
